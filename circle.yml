# circle.yml
machine:
  python:
    version: 2.7.3
  services:
    - docker

test:
  pre:
     - pip install coveralls
  override:
     - nosetests tests --with-coverage --cover-package=jsonapi_collections
  post:
     - coveralls
