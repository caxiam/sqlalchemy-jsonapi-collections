# circle.yml
machine:
  python:
    version: 3.5.1
  services:
    - docker

test:
  pre:
     - pip install coveralls
  override:
     - nosetests tests --with-coverage --cover-package=jsonapi_query
  post:
     - coveralls
